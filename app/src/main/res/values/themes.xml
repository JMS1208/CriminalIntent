<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.20220327_CriminalIntent" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Brown</item>
        <item name="colorPrimaryVariant">@color/lightBrown</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/lightGreen</item>
        <item name="colorSecondaryVariant">@color/cloudyOrange</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="MyCustomDatePicker" parent="Theme.AppCompat.Light.Dialog">
        <!-- 상단 색 설정 -->
        <item name="colorPrimary">@color/lightBrown</item>
        <item name="colorAccent">@color/Brown</item>

        <!-- 배경색 설정 -->
        <item name="background">@color/white</item>

        <!-- 선택한 날짜 색상 설정 -->
        <item name="android:textColor">@color/black</item>
        <item name="colorControlHighlight">@color/brightGrey</item>
        <item name="colorControlActivated">@color/lightGreen</item>

        <item name="android:windowIsFloating">true</item>
    </style>
</resources>